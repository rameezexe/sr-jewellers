# ─────────────────────────────────────────────────────────────────────────
#  Copy this file to ".env" and fill in real values.
#  Never commit .env (it's gitignored).
# ─────────────────────────────────────────────────────────────────────────

# --- Database: Neon Postgres (use the POOLED connection string) ---
# Create a free DB at https://neon.tech and copy the "Pooled connection" URL.
# Must keep ?sslmode=require. We talk to it over HTTPS/WebSocket (port 443).
DATABASE_URL="postgresql://USER:PASSWORD@ep-xxxx-pooler.REGION.aws.neon.tech/neondb?sslmode=require"

# --- Admin login (seeded into the DB by `npm run db:setup`) ---
ADMIN_EMAIL="mom@example.com"
ADMIN_PASSWORD="change-this-now"
ADMIN_NAME="Mom"

# --- Session signing secret (any random 32+ char string) ---
# Generate one with:  node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
AUTH_SECRET="replace-with-a-long-random-string"

# --- Razorpay (https://dashboard.razorpay.com) ---
# Use TEST keys (rzp_test_...) while developing; swap to LIVE keys after KYC.
RAZORPAY_KEY_ID="rzp_test_xxxxxxxx"
RAZORPAY_KEY_SECRET="xxxxxxxxxxxxxxxx"
# Set after creating a webhook in the Razorpay dashboard (see README).
RAZORPAY_WEBHOOK_SECRET=""

# --- Cloudinary (https://cloudinary.com — free tier) ---
CLOUDINARY_CLOUD_NAME=""
CLOUDINARY_API_KEY=""
CLOUDINARY_API_SECRET=""

# --- Resend email (https://resend.com — free tier) ---
# Leave blank to skip emails in local dev. EMAIL_FROM must be a verified domain
# in production; "onboarding@resend.dev" works for testing.
RESEND_API_KEY=""
EMAIL_FROM="Seoul Sparkle <onboarding@resend.dev>"
OWNER_EMAIL="mom@example.com"

# --- Public site URL (used in emails / SEO). Set on Vercel in production. ---
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
